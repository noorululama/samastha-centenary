/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment the following line if you want to export as static HTML
  // output: 'export',
};

export default nextConfig;
